"use client"

import { ModalBackdrop as StyledModalBackdrop } from "./styles/ModalBackdrop.styled"

const ModalBackdrop = () => {
    return <StyledModalBackdrop></StyledModalBackdrop>
}

export default ModalBackdrop

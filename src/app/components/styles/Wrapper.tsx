"use client"

import styled from "styled-components"

export const Wrapper = styled.main`
    width: 90%;
    height: min-content;
    margin: 5em auto 0;
    overflow-y: hidden;
    position: relative;
`
